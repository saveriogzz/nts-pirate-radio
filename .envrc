# This project locks against public PyPI (see [[tool.uv.index]] in
# pyproject.toml). Machine-level corporate index config would override it
# and silently rewrite uv.lock to URLs that CI and the balena builders
# cannot reach, so drop it inside this project.
unset UV_DEFAULT_INDEX
unset UV_INDEX_URL
unset PIP_INDEX_URL
